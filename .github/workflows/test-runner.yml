name: Test Runner

on:
    workflow_dispatch:

jobs:
    test:
        runs-on: self-hosted

        steps:
            - name: Show runner info
              run: |
                  echo "Runner is working"
                  hostname
                  whoami
                  pwd
